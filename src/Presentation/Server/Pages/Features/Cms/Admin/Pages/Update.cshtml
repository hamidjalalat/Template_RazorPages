@page
@model Server.Pages.Features.Cms.Admin.Pages.UpdateModel

@{
	var pageTitle =
		$"{Resources.DataDictionary.UpdateOf} {Resources.DataDictionary.Page}";

	ViewData[Constants.ViewDataKeyName.PageTitle] = pageTitle;
}

<form method="post">

	<input type="hidden" asp-for="ViewModel.Id" />

	<section-form>
		<fieldset>
			<section-form-header>
				@(pageTitle)
			</section-form-header>

			<partial name="PartialViews/_DisplayPageMessages" />

			<dtat-full-checkbox asp-for="ViewModel.IsActive" />

			<dtat-full-input asp-for="ViewModel.Name" dir="ltr" />

			<dtat-full-input asp-for="ViewModel.Ordering" />

			<dtat-full-select asp-for="ViewModel.LayoutId" asp-items="@(Model.LayoutsSelectList)" />

			<dtat-full-input asp-for="ViewModel.Title" />

			<dtat-full-checkbox asp-for="ViewModel.DoesSearchEnginesIndexIt" />

			<dtat-full-checkbox asp-for="ViewModel.DoesSearchEnginesFollowIt" />

			<dtat-full-textarea asp-for="ViewModel.Body" dir="ltr" />

			<dtat-full-input asp-for="ViewModel.Description" />
		</fieldset>

		<section-form-buttons>
			<button-save />
			<button-reset />
		</section-form-buttons>

		<section-form-footer>
			<a asp-page="Index">
				@(Html.DtatGetLinkCaptionForList())
			</a>
			<a asp-page="Details" asp-route-id="@(Model.ViewModel.Id)">
				@(Html.DtatGetLinkCaptionForDetails())
			</a>
			<a asp-page="Delete" asp-route-id="@(Model.ViewModel.Id)">
				@(Html.DtatGetLinkCaptionForDelete())
			</a>
		</section-form-footer>
	</section-form>

</form>
